const Api=(()=>{async function call(action,params={}){const url=new URL(CONFIG.API_URL);url.searchParams.set('action',action);url.searchParams.set('key',CONFIG.API_KEY);Object.entries(params).forEach(([k,v])=>{if(v!==undefined&&v!==null&&v!=='')url.searchParams.set(k,v)});const res=await fetch(url.toString());if(!res.ok)throw new Error(`API error ${res.status}`);return res.json()}async function analyze(m){return call('analyze',{sheet:m.sheet,columns:m.columns||''})}async function readPage(m,page=1){const data=await call('read',{sheet:m.sheet,columns:m.columns||'',limit:CONFIG.PAGE_SIZE,page});if(!data.success)throw new Error(data.error||`Error leyendo ${m.sheet}`);return{rows:(data.data||[]).map(r=>({...r,_sheet:m.sheet})),totalPages:Number(data.total_pages||1),total:Number(data.total||data.count||0),page}}return{call,analyze,readPage}})();