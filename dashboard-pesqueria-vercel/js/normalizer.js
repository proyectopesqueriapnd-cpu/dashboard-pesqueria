const Normalizer=(()=>{const backlogHeaders=['UBICACIÓN','Fluido','Especificación','% PND','JUNTAS SW,LET TOTALES DE PROYECTO','JUNTAS INSPECCIONADAS','JUNTAS REQUERIDAS','BACKLOG'];function isBacklogHeaderRow(row){const vals=Object.values(row).map(v=>Utils.norm(v));return vals.includes('ubicacion')&&vals.includes('fluido')&&vals.some(v=>v.includes('especificacion'))}function normalizeBacklog(rows){if(!rows.length)return rows;let headerIndex=rows.findIndex(isBacklogHeaderRow);if(headerIndex<0){return rows.map(r=>{const keys=Object.keys(r);const vals=Object.values(r);const out={...r};backlogHeaders.forEach((h,i)=>{if(out[h]===undefined&&vals[i]!==undefined)out[h]=vals[i]});return out})}const headerVals=Object.values(rows[headerIndex]).map(v=>String(v||'').trim()).filter(Boolean);const headers=headerVals.length>=4?headerVals:backlogHeaders;return rows.slice(headerIndex+1).map(r=>{const vals=Object.values(r);const out={_sheet:r._sheet};headers.forEach((h,i)=>out[h]=vals[i]??'');return out}).filter(r=>Object.values(r).some(v=>String(v||'').trim()!==''))}function normalize(moduleId,rows){if(moduleId==='backlog')return normalizeBacklog(rows);return rows}return{normalize}})();