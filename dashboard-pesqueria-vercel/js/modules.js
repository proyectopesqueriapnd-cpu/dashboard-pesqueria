const Modules=(()=>{const fields={general:{planta:['Planta'],area:['Area','Área'],linea:['Línea','Linea'],localizacion:['Localizacion','Localización'],statusPT:['Status PT','Estatus PT'],ejecutantePT:['Ejecutante / PT','Ejecutante PT','Ejecutante/PT'],fechaPT:['Fecha / PT','Fecha PT','Fecha/PT']},backlog:{ubicacion:['UBICACIÓN','Ubicación','Ubicacion'],fluido:['Fluido'],especificacion:['Especificación','Especificacion'],pnd:['% PND','%PND'],totalProyecto:['JUNTAS SW,LET TOTALES DE PROYECTO','TOTAL JUNTAS SW,LET CARGADAS EN SISTEMA CONTROLTUB'],inspeccionadas:['JUNTAS INSPECCIONADAS'],requeridas:['JUNTAS REQUERIDAS'],backlog:['BACKLOG']}};function filters(moduleId){if(moduleId==='general')return[{id:'planta',label:'Planta',getter:r=>Utils.getVal(r,fields.general.planta),all:'Todas'},{id:'area',label:'Área',getter:r=>Utils.getVal(r,fields.general.area),all:'Todas'},{id:'statusPT',label:'Status PT',getter:r=>Utils.getVal(r,fields.general.statusPT),all:'Todos',format:Utils.statusLabel},{id:'ejecutantePT',label:'Ejecutante PT',getter:r=>Utils.getVal(r,fields.general.ejecutantePT),all:'Todos'}];return[{id:'ubicacion',label:'Ubicación',getter:r=>Utils.getVal(r,fields.backlog.ubicacion),all:'Todas'},{id:'fluido',label:'Fluido',getter:r=>Utils.getVal(r,fields.backlog.fluido),all:'Todos'},{id:'especificacion',label:'Especificación',getter:r=>Utils.getVal(r,fields.backlog.especificacion),all:'Todas'},{id:'pnd',label:'% PND',getter:r=>Utils.getVal(r,fields.backlog.pnd),all:'Todos'}]}function kpis(moduleId,rows,meta){if(moduleId==='general'){const total=rows.length,plantas=Utils.countBy(rows,r=>Utils.getVal(r,fields.general.planta)).length,areas=Utils.countBy(rows,r=>Utils.getVal(r,fields.general.area)).length,ejecutantes=Utils.countBy(rows,r=>Utils.getVal(r,fields.general.ejecutantePT)).length,status=Utils.countBy(rows,r=>Utils.getVal(r,fields.general.statusPT)),aprobadas=status.filter(d=>Utils.statusClass(d.value)==='status-a').reduce((s,d)=>s+d.count,0),pendientes=status.filter(d=>Utils.statusClass(d.value)==='status-p').reduce((s,d)=>s+d.count,0);return[{label:'Total visible',value:Utils.fmt(total),sub:`${meta.loadedText}`,icon:'database'},{label:'PT Aprobadas',value:Utils.fmt(aprobadas),sub:`${Utils.pct(aprobadas,total)}%`,icon:'check-circle'},{label:'PT Pendientes',value:Utils.fmt(pendientes),sub:`${Utils.pct(pendientes,total)}%`,icon:'alert-triangle'},{label:'Áreas',value:Utils.fmt(areas),sub:`${plantas} plantas`,icon:'map'},{label:'Ejecutantes',value:Utils.fmt(ejecutantes),sub:'Ejecutante / PT',icon:'users'}]}const totalProyecto=Utils.sumBy(rows,r=>Utils.getVal(r,fields.backlog.totalProyecto)),inspeccionadas=Utils.sumBy(rows,r=>Utils.getVal(r,fields.backlog.inspeccionadas)),requeridas=Utils.sumBy(rows,r=>Utils.getVal(r,fields.backlog.requeridas)),backlog=Utils.sumBy(rows,r=>Utils.getVal(r,fields.backlog.backlog)),avance=requeridas?Utils.pct(inspeccionadas,requeridas):Utils.pct(inspeccionadas,totalProyecto);return[{label:'Juntas proyecto',value:Utils.fmt(totalProyecto),sub:`${Utils.fmt(rows.length)} líneas`,icon:'database'},{label:'Inspeccionadas',value:Utils.fmt(inspeccionadas),sub:`${avance}% avance`,icon:'check-circle'},{label:'Requeridas',value:Utils.fmt(requeridas),sub:'Según % PND',icon:'target'},{label:'Backlog PT',value:Utils.fmt(backlog),sub:'Juntas pendientes por inspeccionar',icon:'alert-triangle'},{label:'Ubicaciones',value:Utils.fmt(Utils.countBy(rows,r=>Utils.getVal(r,fields.backlog.ubicacion)).length),sub:'Con backlog PT',icon:'map'}]}function chartCards(moduleId){if(moduleId==='general')return[{id:'status',title:'Distribución Status PT',sub:'Aprobado, pendiente y otros',type:'compact'},{id:'statusPlanta',title:'Status PT por Planta',sub:'Comparativo apilado',type:''},{id:'planta',title:'Registros por Planta',sub:'Distribución general',type:''},{id:'areas',title:'Top Áreas',sub:'Áreas con más registros',type:'compact'},{id:'ejecutantes',title:'Top Ejecutantes PT',sub:'Ranking de ejecutantes',type:''},{id:'fechas',title:'Tendencia por Fecha PT',sub:'Últimas fechas con datos',type:'full tall'}];return[{id:'topUbicacionesBacklog',title:'Top 10 Ubicaciones con mayor Backlog',sub:'Prioridad para programación de inspección',type:'full tall'},{id:'fluido',title:'Backlog por Fluido',sub:'Dónde se concentra el rezago',type:''},{id:'especificacion',title:'Backlog por Especificación',sub:'Top especificaciones con rezago',type:''},{id:'avance',title:'Avance del Backlog PT',sub:'Inspeccionadas vs pendiente por inspeccionar',type:'compact'},{id:'ubicacion',title:'Juntas requeridas por ubicación',sub:'Carga de inspección requerida',type:''},{id:'rankingBacklog',title:'Ranking Detallado de Backlog',sub:'Mayor pendiente por ubicación / fluido / especificación',type:'full tall'}]}function renderCharts(moduleId,rows,actions={}){if(!rows.length)return;if(moduleId==='general'){const status=Utils.countBy(rows,r=>Utils.getVal(r,fields.general.statusPT));Charts.doughnut('chart-status',status.map(d=>Utils.statusLabel(d.value)),status.map(d=>d.count));const plantas=Utils.countBy(rows,r=>Utils.getVal(r,fields.general.planta)).slice(0,10).map(d=>d.value),statuses=status.slice(0,5).map(d=>d.value);Charts.stackedBar('chart-statusPlanta',plantas,statuses.map(s=>({label:Utils.statusLabel(s),data:plantas.map(p=>rows.filter(r=>String(Utils.getVal(r,fields.general.planta))===p&&String(Utils.getVal(r,fields.general.statusPT))===s).length)})));const plantaDist=Utils.countBy(rows,r=>Utils.getVal(r,fields.general.planta));Charts.bar('chart-planta',plantaDist.map(d=>d.value),plantaDist.map(d=>d.count));const areaDist=Utils.countBy(rows,r=>Utils.getVal(r,fields.general.area)).slice(0,15);Charts.doughnut('chart-areas',areaDist.map(d=>d.value),areaDist.map(d=>d.count));const ejecutantes=Utils.countBy(rows,r=>Utils.getVal(r,fields.general.ejecutantePT)).slice(0,15);Charts.bar('chart-ejecutantes',ejecutantes.map(d=>Utils.short(d.value,22)),ejecutantes.map(d=>d.count),true);const fechas=Utils.countBy(rows,r=>Utils.getVal(r,fields.general.fechaPT)).sort(Utils.dateSort).slice(-60);Charts.line('chart-fechas',fechas.map(d=>d.value),fechas.map(d=>d.count));return}const backlogByUbic=new Map();rows.forEach(r=>{const u=String(Utils.getVal(r,fields.backlog.ubicacion)||'').trim();const b=Utils.num(Utils.getVal(r,fields.backlog.backlog));if(u&&b>0)backlogByUbic.set(u,(backlogByUbic.get(u)||0)+b)});const topUbic=[...backlogByUbic.entries()].map(([value,count])=>({value,count})).sort((a,b)=>b.count-a.count).slice(0,10);Charts.bar('chart-topUbicacionesBacklog',topUbic.map(d=>Utils.short(d.value,28)),topUbic.map(d=>d.count),true,(label,index)=>{if(actions.filterBacklogUbicacion)actions.filterBacklogUbicacion(topUbic[index].value)});const byFluidoMap=new Map();rows.forEach(r=>{const f=String(Utils.getVal(r,fields.backlog.fluido)||'').trim();const b=Utils.num(Utils.getVal(r,fields.backlog.backlog));if(f&&b>0)byFluidoMap.set(f,(byFluidoMap.get(f)||0)+b)});const byFluido=[...byFluidoMap.entries()].map(([value,count])=>({value,count})).sort((a,b)=>b.count-a.count).slice(0,12);Charts.bar('chart-fluido',byFluido.map(d=>Utils.short(d.value,24)),byFluido.map(d=>d.count),true);const byEspMap=new Map();rows.forEach(r=>{const e=String(Utils.getVal(r,fields.backlog.especificacion)||'').trim();const b=Utils.num(Utils.getVal(r,fields.backlog.backlog));if(e&&b>0)byEspMap.set(e,(byEspMap.get(e)||0)+b)});const byEsp=[...byEspMap.entries()].map(([value,count])=>({value,count})).sort((a,b)=>b.count-a.count).slice(0,15);Charts.bar('chart-especificacion',byEsp.map(d=>Utils.short(d.value,24)),byEsp.map(d=>d.count),true);const inspeccionadas=Utils.sumBy(rows,r=>Utils.getVal(r,fields.backlog.inspeccionadas)),backlog=Utils.sumBy(rows,r=>Utils.getVal(r,fields.backlog.backlog));Charts.doughnut('chart-avance',['Inspeccionadas','Backlog PT'],[inspeccionadas,backlog]);const reqByUbic=new Map();rows.forEach(r=>{const u=String(Utils.getVal(r,fields.backlog.ubicacion)||'').trim();const req=Utils.num(Utils.getVal(r,fields.backlog.requeridas));if(u&&req>0)reqByUbic.set(u,(reqByUbic.get(u)||0)+req)});const reqUbic=[...reqByUbic.entries()].map(([value,count])=>({value,count})).sort((a,b)=>b.count-a.count).slice(0,15);Charts.bar('chart-ubicacion',reqUbic.map(d=>Utils.short(d.value,24)),reqUbic.map(d=>d.count),true);const ranking=rows.map(r=>({label:`${Utils.getVal(r,fields.backlog.ubicacion)} · ${Utils.getVal(r,fields.backlog.fluido)} · ${Utils.getVal(r,fields.backlog.especificacion)}`,value:Utils.num(Utils.getVal(r,fields.backlog.backlog))})).filter(d=>d.value>0).sort((a,b)=>b.value-a.value).slice(0,25);Charts.bar('chart-rankingBacklog',ranking.map(d=>Utils.short(d.label,36)),ranking.map(d=>d.value),true)}function tableColumns(moduleId,rows){const first=rows[0]||{};const keys=Object.keys(first);if(moduleId==='general'){const preferred=['Planta','Area','Área','Localizacion','Localización','Línea','Linea','Junta','Status PT','Ejecutante / PT','Fecha / PT'];return preferred.map(p=>keys.find(k=>Utils.norm(k)===Utils.norm(p))).filter(Boolean).slice(0,10)}const preferred=['UBICACIÓN','Fluido','Especificación','% PND','JUNTAS SW,LET TOTALES DE PROYECTO','JUNTAS INSPECCIONADAS','JUNTAS REQUERIDAS','BACKLOG'];return preferred.map(p=>keys.find(k=>Utils.norm(k)===Utils.norm(p))).filter(Boolean).slice(0,10)}return{fields,filters,kpis,chartCards,renderCharts,tableColumns}})();